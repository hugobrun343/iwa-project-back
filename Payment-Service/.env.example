# Example environment variables for stripe-backend
# Fill these in your local `.env` (DO NOT commit real secrets)

# Stripe keys
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# Server port (optional)
PORT=8088

# Database URL (Postgres) — used by Prisma when you want a persisted DB
# Example: postgres://postgres:postgres@db:5432/stripebackend
# If you don't intend to use a local Postgres instance for dev, leave empty.
DATABASE_URL=

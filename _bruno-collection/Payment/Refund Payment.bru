meta {
  name: Refund Payment
  type: http
  seq: 9
}

post {
  url: {{GATEWAY_URL}}/api/payments/refund
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

body:json {
  {
    "paymentIntentId": "{{PAYMENT_INTENT_ID}}",
    "amount": 10000,
    "reason": "requested_by_customer"
  }
}

script:post-response {
  if (res.status === 200) {
    console.log('✅ Payment refunded successfully');
    console.log('💸 Refund ID:', res.body.refundId);
    console.log('💰 Amount:', res.body.amount / 100, res.body.currency);
    console.log('📊 Status:', res.body.status);
  } else {
    console.error('❌ Failed to refund payment:', res.status, res.body);
  }
}

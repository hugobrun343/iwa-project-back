meta {
  name: Get Favorites By Announcement
  type: http
  seq: 5
}

get {
  url: {{GATEWAY_URL}}/api/favorites/announcement/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

script:post-response {
  if (res.status === 200) {
    console.log(`✅ Retrieved ${Array.isArray(res.body) ? res.body.length : 0} favorites for this announcement`);
    if (Array.isArray(res.body) && res.body.length > 0) {
      console.log('📋 Favorites:', res.body);
    }
  } else {
    console.error('❌ Failed to get favorites:', res.status);
  }
}
